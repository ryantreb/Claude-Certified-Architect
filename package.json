{
  "name": "weavefall",
  "version": "1.0.0",
  "private": true,
  "description": "Weavefall — a single-file dark-fantasy study RPG for two agentic-AI certifications.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "python3 -m http.server 8753"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
